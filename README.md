# net-android-debugger-profiler

A debugger and a profiler for .NET for Android applications, kept in one repository.
This root README is written in phase 4 of the merge; until then the two products are
described by `docs/debugger/README.md` and `docs/profiler/README.md`.
